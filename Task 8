function sum() {
    var array = Array.from(arguments);
    if (array.length > 1)
        return array[0] + sum(...array.slice(1));
    else
        return array[0];
}

console.log(sum(1,3,5,7));

/*result::
16
*/
